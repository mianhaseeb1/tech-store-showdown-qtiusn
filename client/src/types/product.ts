export type Product = {
    id: string
    name: string
    price: number
    quantity: number
    description: string
}

export type NewProduct = {
    name: string
    description: string
    price: number
    quantity: number
}
